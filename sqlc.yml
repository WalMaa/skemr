version: "2"
sql:
  - engine: "postgresql"
    queries: "./queries"
    schema: "schema.sql"
    gen:
      go:
        package: "skemr"
        emit_json_tags: true
        out: "db"
        sql_package: "pgx/v5"